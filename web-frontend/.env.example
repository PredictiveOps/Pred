# Environment variables for Keycloak
# Place in web-frontend/.env.local with your actual values

# ============================================
# NextAuth Configuration
# ============================================

# URL where your Next.js app runs
NEXTAUTH_URL=http://localhost:3000

# Secret for encrypting session tokens
# Generate a random string at least 32 characters long
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NEXTAUTH_SECRET=your-32-character-secret-key-here-change-in-production

# ============================================
# Keycloak Configuration
# ============================================

# URL where Keycloak server is running
KEYCLOAK_URL=http://localhost:8080

# Realm where your users live. Automatically provisioned by configure.sh when cnotainer is created
KEYCLOAK_REALM=prod-maintenance

# Client ID provisioned by configure.sh (case-sensitive).
KEYCLOAK_CLIENT_ID=web-frontend

# Client secret. The dev value below matches the default the keycloak-config sidecar
# sets in docker-compose.yml. Override both (KEYCLOAK_CLIENT_SECRET in compose env
# and here) with a real secret in production.
KEYCLOAK_CLIENT_SECRET=dev-web-frontend-secret

# ============================================
# Optional: Custom Configuration
# ============================================

# Uncomment to use HTTPS in production
# KEYCLOAK_URL=https://keycloak.yourdomain.com
# NEXTAUTH_URL=https://app.yourdomain.com
