name: CI - Notifications Service

on:
  push:
    branches:
      - main
    paths:
      - "notifications-service/**"
      - ".github/workflows/ci-notifications-service.yaml"

  pull_request:
    branches:
      - main
    paths:
      - "notifications-service/**"
      - ".github/workflows/ci-notifications-service.yaml"

  workflow_dispatch:

env:
  SERVICE_NAME: notifications-service
  IMAGE_TAG: ${{ github.sha }}

permissions:
  contents: read
  id-token: write

jobs:
  Build:
    name: Build Go Service
    runs-on: ubuntu-latest

    steps:
      - name: Code checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: notifications-service/go.mod
          cache-dependency-path: notifications-service/go.sum

      - name: Download Go dependencies
        working-directory: notifications-service
        run: go mod download

      - name: Build notifications service
        working-directory: notifications-service
        run: go build ./...

      - name: Notify if build fails
        if: failure()
        run: echo "Notifications service build failed. Check the logs."

  Testing:
    name: Test Go Service
    runs-on: ubuntu-latest
    needs: Build

    steps:
      - name: Code checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: notifications-service/go.mod
          cache-dependency-path: notifications-service/go.sum

      - name: Run Go tests
        working-directory: notifications-service
        run: go test ./...

      - name: Notify if tests fail
        if: failure()
        run: echo "Notifications service tests failed. Check the logs."

  Security-Scan:
    name: Trivy Filesystem Scan
    runs-on: ubuntu-latest
    needs: Build

    steps:
      - name: Code checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Trivy filesystem scan
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: filesystem
          scan-ref: notifications-service
          format: json
          output: trivy-notifications-service-results.json
          exit-code: "0"
          vuln-type: os,library
          severity: CRITICAL,HIGH

      - name: Upload Trivy scan results
        uses: actions/upload-artifact@v4
        with:
          name: trivy-notifications-service-results
          path: trivy-notifications-service-results.json

  BUILD_AND_PUBLISH:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    needs: [Build, Testing, Security-Scan]

    if: github.event_name == 'push'

    steps:
      - name: Code checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.WIF_PROVIDER }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}

      - name: Set up Google Cloud CLI
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: |
          gcloud auth configure-docker ${{ vars.GCP_REGION }}-docker.pkg.dev --quiet

      - name: Build Docker image
        run: |
          docker build \
            -f notifications-service/Dockerfile \
            -t ${{ vars.GCP_REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}/${{ vars.GAR_REPOSITORY }}/${{ env.SERVICE_NAME }}:${{ env.IMAGE_TAG }} \
            notifications-service

      - name: Run Trivy image scan
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ vars.GCP_REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}/${{ vars.GAR_REPOSITORY }}/${{ env.SERVICE_NAME }}:${{ env.IMAGE_TAG }}
          format: table
          exit-code: "0"
          vuln-type: os,library
          severity: CRITICAL,HIGH

      - name: Push Docker image to Artifact Registry
        run: |
          docker push ${{ vars.GCP_REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}/${{ vars.GAR_REPOSITORY }}/${{ env.SERVICE_NAME }}:${{ env.IMAGE_TAG }}
