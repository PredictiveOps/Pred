name: CI - Event Processing Service

on:
  push:
    branches:
      - main
    paths:
      - "event-processing-service/**"
      - ".github/workflows/ci-event-processing-service.yaml"

  pull_request:
    branches:
      - main
    paths:
      - "event-processing-service/**"
      - ".github/workflows/ci-event-processing-service.yaml"

  workflow_dispatch:

env:
  SERVICE_NAME: event-processing-service
  IMAGE_TAG: ${{ github.sha }}

permissions:
  contents: read
  id-token: write

jobs:
  Build:
    name: Build Go Service
    runs-on: ubuntu-latest

    steps:
      - name: Code checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: event-processing-service/go.mod
          cache-dependency-path: event-processing-service/go.sum

      - name: Download Go dependencies
        working-directory: event-processing-service
        run: go mod download

      - name: Build event processing service
        working-directory: event-processing-service
        run: go build ./...

      - name: Notify if build fails
        if: failure()
        run: echo "Event processing service build failed. Check the logs."

  Testing:
    name: Test Go Service
    runs-on: ubuntu-latest
    needs: Build

    steps:
      - name: Code checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: event-processing-service/go.mod
          cache-dependency-path: event-processing-service/go.sum

      - name: Run Go tests
        working-directory: event-processing-service
        run: go test ./...

      - name: Notify if tests fail
        if: failure()
        run: echo "Event processing service tests failed. Check the logs."

  Security-Scan:
    name: Trivy Filesystem Scan
    runs-on: ubuntu-latest
    needs: Build

    steps:
      - name: Code checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Trivy filesystem scan
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: filesystem
          scan-ref: event-processing-service
          format: json
          output: trivy-event-processing-service-results.json
          exit-code: "0"
          vuln-type: os,library
          severity: CRITICAL,HIGH

      - name: Upload Trivy scan results
        uses: actions/upload-artifact@v4
        with:
          name: trivy-event-processing-service-results
          path: trivy-event-processing-service-results.json

  BUILD_AND_PUBLISH:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    needs: [Build, Testing, Security-Scan]

    if: github.event_name == 'push'

    steps:
      - name: Code checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.WIF_PROVIDER }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}

      - name: Set up Google Cloud CLI
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: |
          gcloud auth configure-docker ${{ vars.GCP_REGION }}-docker.pkg.dev --quiet

      - name: Build Docker image
        run: |
          docker build \
            -f event-processing-service/Dockerfile \
            -t ${{ vars.GCP_REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}/${{ vars.GAR_REPOSITORY }}/${{ env.SERVICE_NAME }}:${{ env.IMAGE_TAG }} \
            event-processing-service

      - name: Run Trivy image scan
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ vars.GCP_REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}/${{ vars.GAR_REPOSITORY }}/${{ env.SERVICE_NAME }}:${{ env.IMAGE_TAG }}
          format: table
          exit-code: "0"
          vuln-type: os,library
          severity: CRITICAL,HIGH

      - name: Push Docker image to Artifact Registry
        run: |
          docker push ${{ vars.GCP_REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}/${{ vars.GAR_REPOSITORY }}/${{ env.SERVICE_NAME }}:${{ env.IMAGE_TAG }}

  Update-GitOps:
    name: Update GitOps Image Tag
    runs-on: ubuntu-latest
    needs: [BUILD_AND_PUBLISH]

    steps:
      - name: Clone GitOps repo
        uses: actions/checkout@v4
        with:
          repository: PredictiveOps/Pred-Gitops
          token: ${{ secrets.GITOPS_PAT }}
          path: gitops

      - name: Update image tag
        run: |
          sed -i "s|${{ vars.GCP_REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}/${{ vars.GAR_REPOSITORY }}/${{ env.SERVICE_NAME }}:.*|${{ vars.GCP_REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}/${{ vars.GAR_REPOSITORY }}/${{ env.SERVICE_NAME }}:${{ env.IMAGE_TAG }}|g" \
            gitops/apps/${{ env.SERVICE_NAME }}/deployment.yaml

      - name: Commit and push
        run: |
          cd gitops
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add apps/${{ env.SERVICE_NAME }}/deployment.yaml
          git commit -m "ci: update ${{ env.SERVICE_NAME }} image to ${{ env.IMAGE_TAG }}"
          git push
